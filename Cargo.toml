[package]
name = "pokeball-rs"
version = "0.3.0-alpha.2"
edition = "2021"
license = "MIT"
description = "Pokemon Go Plus library"
repository = "https://github.com/Jesus805/pokeball-rs"
readme = "README.md"


[dependencies]
uuid = {version = "1.12.0", optional = true}

[features]
default = []
full = ["uuid"]
uuid = ["dep:uuid"]
